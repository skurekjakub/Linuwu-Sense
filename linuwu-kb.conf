# Linuwu-Sense keyboard enforcer configuration
# Mode: "off" = backlight off, "per-zone" = static per-zone color
MODE="off"
# Per-zone hex colors (zone1,zone2,zone3,zone4)
PZ_COLORS="ffffff,ffffff,ffffff,ffffff"
# Brightness (0-100)
PZ_BRIGHTNESS=10
